{
  "name": "countdown-gif-generator",
  "version": "1.0.0",
  "description": "Countdown GIF Generator API",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "postinstall": "DEBIAN_FRONTEND=noninteractive apt-get update && apt-get install -y fontconfig fonts-dejavu-core"
  },
  "dependencies": {
    "canvas": "^3.0.1",
    "express": "^4.21.2",
    "gif-encoder": "^0.7.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.3"
  }
}
